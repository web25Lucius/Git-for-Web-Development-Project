
Copy the questions below into the firt-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? using tags that carry meaning and allow optimization as well as greater accesibility for screen readers. 

    2. What is HTML used for? structure a website

    3. What is an attribute and where do we put it? assigned to elements so we can further customize them 

    4. What is the h1 tag used for? How many times should I use it on a page? Once, to create a main heading

    5. Name two tags that have required attributes:  <a>, <img>

    6. What do we put in the head of our HTML document? meta tags, title

    7. What is an id? used once to style a specific element 

    8. What elements can I add an id to? any 

    9. How many times can I use the same id on a page? use is once

    10. What is a class? class=""  allowing you to select specific elements and style them 

    11. What elements can I add a class to? any 

    12. How many times can I use the same class on a page? as many as you like 

    13. How do I get my link to open in a new tab? target 

    14. What is the alt attribute used for? screen readers 

    15. How do I reference an id? #

    16. What is the difference between a section and a div? 
    semantics 

    17. What is CSS used for? styling 

    18. How to we select an element? Example - every h2 on the page
    h2{}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
.class is called to style a select group of elements on a page
#id is used to style one specific element on a page

    20. How do we select classes in CSS? .nameOfClass

    21. How do we select a p element with a single class of “human””?
    p .human{}

    22. What is a parent child selector? When would this be useful?
    A way to select nested elements.  Use when elements are nested.  


    23. How do you select all links within a div with the class of sidebar?
<div class="sidebar">
<a href="#">links</a>
</div>

.sidebar a{}

    24. What is a pseudo selector?  last child, nth child, first child, :hover, etc.

    25. What do we use the change the spacing between lines? line height 

    26. What do we use to change the spacing between letters? letter spacing 

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    .toUpperCase(),  .toLowerCase(), you can also use CSS styling.  

    28. How do I add a 1px border around my div that is dotted and black?
    there isn't one way to do any of these things-  you could style the border like our most recent the codepen assignment 

    
    selector {
        border: 1px black dotted; 
    }

    29. How do I select everything on the page? *

    30. How do I write a comment in CSS?
/**/

    31. How do I find out what file I am in, when I am using the command line? read it  

    32. Using the command line - how do I see a list of files/folders in my current folder?
    ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    cntrl z can't help you if you remove something.  
    use the bash language to give the command to remove the file.  All commands can be found by googling. 

    34. Why should I use version control? 
    so you can roll back if you make a change you don't like, or bug hunt on another branch 

    35. How often should I commit to github? whenever you makek a meaningful change you don't want to lose 

    36. What is the command we would use to push our repo up to github? git push origin  
    
    37. Walk me through Lambda's git flow. 
    fork, add collaborator, clone, change branch, work, add, commit, push, pull request 

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model? 

